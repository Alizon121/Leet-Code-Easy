function getCount(objects) {
    // We need to count how may keys === value
    
    let count= 0;
    
    for (let key in objects) {
        // console.log(key)
        
    }
    // return count
}


